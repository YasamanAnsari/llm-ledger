# Lab marks

One SVG per `org_id` in `data/core/organizations.csv`.

Path: `assets/orgs/{org_id}.svg`.

Where we could find a Simple Icons (CC0) or models.dev mark, we used
that. The rest are letter marks so every lab still has a file. Replace
a letter mark with an official SVG anytime; keep the filename.
